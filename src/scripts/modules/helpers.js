class ObjectCreatror {
  constructor(dayName, icon, weather, maxTemp, minTemp, windSpeed) {
    this.dayName = dayName;
    this.icon = icon;
    this.weather = weather;
    this.maxTemp = maxTemp;
    this.minTemp = minTemp;
    this.windSpeed = windSpeed;
  }
}

export default ObjectCreatror;
